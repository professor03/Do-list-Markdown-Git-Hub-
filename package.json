{
  "name": "pomodoro-board",
  "private": true,
  "workspaces": ["client", "server"],
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "npm -w server run dev & npm -w client run dev"
  }
}
